# tiny-transformer-language-model
A minimal Transformer-based language model built from scratch in PyTorch, trained on the Tiny Shakespeare dataset for next-token prediction. Includes attention visualization, perplexity evaluation, and hyperparameter analysis.
